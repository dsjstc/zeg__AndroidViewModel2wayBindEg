<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:bind="http://schemas.android.com/apk/res-auto">
    <data>
    </data>

    <LinearLayout
        xmlns:tools="http://schemas.android.com/tools"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight=".95">

            <fragment
                    android:layout_width="200dp"
                    android:layout_height="match_parent"
                    android:name="ca.thundersun.atixpos.FragStatus"
                    android:id="@+id/fragstatus"
                    tools:layout="@layout/frag_status"
                    />

            <fragment
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:name="ca.thundersun.atixpos.FragSales"
                    android:id="@+id/fragsales"
                    tools:layout="@layout/frag_sales"
                />
        </LinearLayout>
        <fragment
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:name="ca.thundersun.atixpos.FragDevbuttons"
            android:id="@+id/frag_devbuttons"
            tools:layout="@layout/frag_devbuttons"
            />
    </LinearLayout>
</layout>
